version: '3.8'

services:
  hopper-sim:
    build: .
    volumes:
      - .:/workspace
    working_dir: /workspace
    environment:
      - PYTHONPATH=/workspace
    stdin_open: true
    tty: true
    command: /bin/bash
